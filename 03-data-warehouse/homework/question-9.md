```sql
-- Scanning 0 MB of Data
SELECT COUNT(*) FROM `taxi_rides_ny.hw3_nonpartitioned_tripdata`;
```

---

```md
0 B.
Because BigQuery optimizes queries on materialized tables by using precomputed metadata.
```
